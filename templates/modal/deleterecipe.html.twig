<div class="modal fade" id="deleteModal{{ recipe.id }}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">Du er ved at slette en opskrift</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Er du sikker på at du vil slette opskriften:
        <br>
        <strong>{{ recipe.name }}</strong>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fortryd</button>
        <a href="{{ path('slet', {'id': recipe.id} ) }}" type="button" class="btn btn-danger">Slet</a>
      </div>
    </div>
  </div>
</div>