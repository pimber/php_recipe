{% extends 'base.html.twig' %}

{% block body %}
    <div class="row d-flex justify-content-center">
        <h3 class="my-5 d-flex justify-content-center text-white">Random Quote</h3>
        <div class="col-3"></div>
        <div class="col-6 mb-4">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Search for recipe" {# Something to search DB and open create modal #}>
            </div>
        </div>
        {# Auth #}
            <div class="col-3 d-flex justify-content-end">
                <button type="button" class="btn btn-success mb-4 me-2" data-bs-toggle="modal" data-bs-target="#staticBackdrop">Create new recipe</button>
            </div>
        {# Auth #}
    </div>
    <div class="row">
        <div class="col-12">
            <div class="row">
                {# Show only the ones the owner has created recipe cards #}
            </div>
        </div>
    </div>

    {# Create recipe modal view here #}

    {# Recipe Modal View here #}
{% endblock %}