{% extends 'base.html.twig' %}

{% block body %}

<div class="row">
    <div class="col-12">
        <h3 class="my-5 d-flex justify-content-center text-white">Todays three random recipies</h3>
        <div class="row d-flex justify-content-center">
            {# @if (complete_recipes == null) { #}
                <p><em>Loading...</em></p>
            {# } else {
                @foreach (CompleteRecipe complete_recipe in complete_recipes) { #}
                    <div class="col-lg-4 col-md-6 col-sm-12 d-flex flex-row justify-content-center mb-4">
                        <div class="card mx-2 mb-2" style="width: 18rem;">
                            <img src="{{ asset('img/boiler.png') }}" class="card-img-top" alt="...">
                            <div class="card-body">
                                <h5 class="card-title"{# >@complete_recipe.name #}</h5>
                                <p class="card-text">{# @complete_recipe.description.Substring(0, Math.Min(60, complete_recipe.description.Length)) #}</p>
                                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop@(complete_recipe.complete_recipe_id)">Go somewhere</button>
                            </div>
                        </div>   
                    </div>
                {# } #}
            {# } #}
        </div>
    </div>
</div>
<div class="row mb-4">
    <div class="col-12">
        <h3 class="my-5 d-flex justify-content-center text-white">Want to learn more about the other recipies?</h3>
        <div class="col-12 d-flex justify-content-center">
            {# @for (int i = 0; i < 1; i++) { #}
            <div class="card col-lg-8 col-md-6 col-sm-6">
                <img src="{{ asset('img/boiler.png') }}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">Card title</h5>
                    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                    <p class="card-text"><small class="text-body-secondary">Last updated 3 mins ago</small></p>
                    <button class="btn btn-primary">Go somewhere</button>
                </div>
            </div>
            {# } #}
        </div>
    </div>
</div>

{% endblock %}