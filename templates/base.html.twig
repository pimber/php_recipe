<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Oldefarsopskrifter{% endblock %}</title>
        <link rel="icon" href="/icons/menu_book.svg">
        <link href="https://fonts.googleapis.com/css?family=Nunito+Sans:400,400i,700,900&display=swap" rel="stylesheet">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="bg-image overflow-auto" style="scrollbar-width: none;">
            <div class="container bg-center d-flex flex-column min-vh-100">
                <div class="row">
                    <div class="col-lg-1 "></div>
                    {# Normal navbar #}
                    <div class="col-lg-10 ">
                        {% include 'components/navbar.html.twig' %}
                    </div>
                    {# Hamburger navbar #}
                    <div class="col-lg-1 d-flex flex-row justify-content-between">
                        {% include 'components/hamburger.html.twig' %}
                    </div>
                </div>
                <div class="row flex-grow-1">
                    <div class="col-12">
                        {% block body %}{% endblock %}
                    </div>
                </div>
                <div class="row">
                    {# Footer #}
                    <div class="col-12">
                        {% include 'components/footer.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>